class gameSettings:
    def __init__(self, screenHeight, screenWidth, playerHeight, ballSize):
        self.screenHeight = screenHeight
        self.screenWidth = screenWidth
        self.playerHeight = playerHeight
        self.ballSize = ballSize