name: Build Docker Containers

on:
  #push:
  #  branches:
  #    - TRA-237-Compile-ModSecurity-Quick-CI-Job-Health-Test  # Adjust the branch as needed
  pull_request:
    branches:
      - dev  # Adjust the branch as needed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build containers
      run: make all